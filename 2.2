ListNode *findNthLast(ListNode *head, int n)
{
	if(head == NULL || n < 1) return NULL;
	ListNode *p1 = head;
	ListNode *p2 = head;
	while(p2 && --n) p2 = p2->next;
	if(n) return NULL;
	while(p2->next){
		p2 = p2->next;
		p1 = p1->next;
	}
	return p1;;
}
